<div data-controller="like" data-liked="<%= current_user&.liked?(shop) %>" 
                              data-id="<%= shop.id %>" class="my-2">
    <%= link_to '#', class: 'w-full bg-gray-400 hover:bg-gray-300 text-black font-semibold py-2 px-4 rounded focus:outline-none focus:shadow-outline', data: { action: 'click->like#toggle',
                                                        like_target: 'btn' } do %>
    <% end %>
</div>