<div data-controller="like" data-liked="<%= current_user&.liked?(shop) %>" 
                              data-id="<%= shop.id %>" class="my-2">
    <%= link_to '#', class: 'btn btn-sm bg-primary text-white hover:bg-accent items-center', data: { action: 'click->like#toggle',
                                                        like_target: 'btn' } do %>
    <% end %>
</div>