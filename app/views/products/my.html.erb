<%= t('vendor.myproduct') %>

<%= link_to t('vendor.create'), new_product_path, class: 'btn btn-primary mb-4' %>

<ul data-controller="drag"
    class="grid grid-cols-1 gap-3 md:grid-cols-2 lg:grid-cols-4">
  <%= render partial: 'item', collection: @products, as: :product %>
</ul>

<footer>
  <%= paginate @products %>
</footer>