<nav class="navbar bg-yellow-100">
  <div class="flex-1">
    <h1 class="mx-2 text-4xl font-bold"><%= link_to "RestTime",root_path %></h1>
        <%= link_to t('navbar.products'), products_path %>
        <%= link_to t('navbar.shops'), shops_path, class: 'ml-2.5' %>
  </div>
  <div class="flex-none">
    <ul class="flex gap-3 menu menu-horizontal px-1">
      <% if user_signed_in? %>
        <li>
          <details>
            <summary>
              <%= current_user.email %>
            </summary>
            <ul class="p-2 bg-base-100 rounded-t-none">
              <li><%= link_to t('navbar.logout'), destroy_user_session_path, data: { turbo_method: "delete" } %></li>
              <li><%= link_to('修改密碼', edit_registration_path(:user)) %></li>
            </ul>
          </details>
        </li>
      <% else %>
        <li><%= link_to '註冊', new_user_registration_path %></li>
        <li><%= link_to '登入', new_user_session_path %></li>
      <% end %>

      <%#是否轉為店家後台判斷%>
      <% if current_user %>
        <%= current_user.role %>
          <%= link_to "logout", destroy_user_session_path, method: :delete, data: { turbo_method: :delete} %>
      <% else %>
          <%= link_to "log_in", new_user_session_path %>
      <% end %>


      <div>
        <form action="<%= search_path %>" data-turbo="false">
          <input type="search" name="q" class="text-black input input-bordered">
          <button class="btn btn-success">搜尋商品</button>
        </form>
      </div>

    </ul>
  </div>
</nav>