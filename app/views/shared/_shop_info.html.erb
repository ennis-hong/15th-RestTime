<ul>
  <li class="mb-4">
    <h2 class="text-2xl text-gray-700 font-bold">
      <%= resource.title %>
    </h2>
    <div class="flex items-center">
    <span class="text-gray-500 mr-2">
      <%= resource.city %><%= resource.district %>
    </span>
    <%= link_to "TEL:#{resource.tel}", "tel:#{resource.tel}", class: 'text-xs badge badge-accent badge-outline pt-0.5' %>
  </div>
  </li>
  <li class="text-m order_text tracking-wide">
    <div class="text-gray-500">
      <%= resource.description %>
    </div>
  </li>
</ul>