<h2>Forgot your password?666</h2>
<div class=w-52>
<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

<div class="form-control">
  <%= f.label :email, t('member.email'), class: "label-text text-lg" %>
  <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "input input-bordered w-52 text-sm px-2 py-3" %>
</div>

<div class="actions">
  <%= f.submit t('member.send_me_reset_password_instructions'), class: "input input-bordered w-52 text-sm px-2 py-3" %>
</div>
<% end %>

  <%= render "devise/shared/links" %>
  </div>